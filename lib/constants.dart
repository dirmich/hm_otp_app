import 'package:flutter/material.dart';

const Color loginBg = Color(0xff00c470);
const Color singupBg = Color(0xff000a54);

const double PADDING = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300);
